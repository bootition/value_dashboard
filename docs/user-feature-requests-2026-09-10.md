# 用户功能需求登记与落实台账（指数菜单 / ETF 策略）

> 2026-09-10 应用户要求建立：把对话中提出的每一项改进登记在案，
> 标注落实位置与验证方式，杜绝"以为完成但界面没有"的情况。
> 状态含义：✅ 已上线并验证；🆕 本轮新完成；⏳ 未开始。

## 一、指数菜单（指数研究页 / 指数详情页）

| # | 需求（用户原意） | 状态 | 落实位置 |
|---|---|---|---|
| 1 | 指数菜单红队审查：可用 + 快 | ✅ | overview 22.6s→<1.5s（bisect 国债对齐+批量预取）；ETF overview 25.7s→<1s |
| 2 | 图表模仿个股详情：十字线跟随光标、时间范围选择 | ✅ | `IndexValuationChart`/`FundamentalChart`（1/3/5/10年/全部） |
| 3 | 十字线右偏，须像 K 线一样精确锁定 | ✅ | 以图表矩形 `getBoundingClientRect` 直接取坐标 |
| 4 | 缩放图片关不掉 | ✅ | Teleport 到 body + 三保险关闭：固定「✕ 收起」按钮 / Esc / 点击背板 |
| 5 | 放大层向右扩张溢出屏幕 | ✅ | fixed 下 `width:100%` 使 `inset right` 失效，改 `width:auto` |
| 6 | 删除"全部"分类标签 | ✅ | `IndexPage` 仅 宽基/红利、申万一级 两个标签 |
| 7 | 全A指数归入宽基（观察全市场） | ✅ | `index_catalog` category=broad；宽基排序第二位 |
| 8 | 从行业指数详情返回后应保持"申万一级"标签 | 🆕 | `IndexPage` 标签持久化到 localStorage（`vd-index-category-tab`） |

## 二、全A指数（按正常 ETF/指数研究）

| # | 需求（用户原意） | 状态 | 落实位置 |
|---|---|---|---|
| 9 | 全A按正常 ETF 研究：PE/PB/ERP 要自己算 | ✅ | `_all_a_index_summary`：总市值/归母TTM、总市值/归母净资产、1/PE−10Y 国债 |
| 10 | PE 这些指标不应只有数值，要有图 | 🆕 | `_all_a_valuation_series`：月末市值×逐股 ASOF 财报对齐，134 个月度点；ERP/PE/PB 三张历史分位图（指数详情页 + ETF 详情弹窗） |
| 11 | 全A市值口径不能用陈旧财报快照 | ✅ | 改用最新日收盘价×总股本（2026-09-09）；页面披露"市值截至/利润报告期"双日期 |
| 12 | 全A在 ETF 面板也要有估值（之前全空） | ✅ | `_effective_track_code`：ALL_A 回退合成指数；概览/详情统计新增 PE/PB/ERP/估值截至 |

## 三、基本面图表（全A及其他 ETF 共用）

| # | 需求（用户原意） | 状态 | 落实位置 |
|---|---|---|---|
| 13 | 行业利润增长贡献可选季度同比/环比 | 🆕 | 数据早已双口径；本轮把选择器直接放到贡献卡片上（指数详情页），两个卡片选择器同步 |
| 14 | 贡献图负值应反向画（向左），不是只标红 | 🆕 | `IndustryContributionBars` 改为中轴正负双向条形 |
| 15 | 按利润贡献率降序，负值排最后 | 🆕 | `sortedItems` 按 contribution_pct 降序 |
| 16 | 贡献图副标题跟随口径（vs 去年同期 / vs 上一报告期） | 🆕 | `periodLabel` 随 mode 切换 |
| 17 | 全市场业绩与市值图分度值最小一年 | 🆕 | `FundamentalChart` 年度聚合：每年取最后一点（利润年内累计=当年值，市值=年末/最新），X 轴按年份标注 |

## 四、ETF 策略面板（普通 ETF）

| # | 需求（用户原意） | 状态 | 落实位置 |
|---|---|---|---|
| 18 | 多个 ETF 详情（如沪深300）加载很久最终失败 | ✅ | 根因：自动更新写 DuckDB 时 Windows 独占文件；改快速失败+缓存旧值降级+启动预热；真实写锁窗口实测 200/30-516ms |
| 19 | ETF 概览慢 | ✅ | 批量摘要/批量收盘价/批量网格状态 |
| 20 | 清空策略数据，真正重新开始 | ✅ | `fresh_start`：清空 meta/交易/现金/设置并重置默认池；数据库已清 |
| 21 | 初始化引导不要等用户吩咐：总资金、持仓录入流程 | ✅ | `EtfStrategyPanel` onboarding 向导 + 资金/持仓弹窗 + `/api/etf/bootstrap` |
| 22 | ETF 详情弹窗基本面卡片（业绩/市值/增速/行业贡献） | ✅ | `fundamental_detail` + 前端卡片（openDetail 对所有 ETF 拉取） |
| 23 | 行业利润贡献口径（申万行业=精确匹配，宽基=规模近似）如实披露 | ✅ | `resolve_constituents` method/disclaimer 展示在卡片上 |
| 26 | 普通 ETF/指数详情页也要"市值与利润的关系"（像全A一样） | 🆕 | 用户明确**不要**放在轮动策略表格（已撤销该处改动）；落地为 `/api/index/{code}/fundamentals` + 每个指数详情页新增"成分股业绩与市值""成分股利润增长率（同比/环比）"板块，meta 行含 市值/利润 倍数；沪深300=规模近似300只、传媒=申万行业131只、全A=5545只 |
| 27 | 放大层退出按钮重复（排查遗留） | 🆕 | 三个图表组件：放大态仅保留右上角固定「✕ 收起」，头部按钮只在未放大时显示「放大」 |

## 五、机制性修复（防复发）

| # | 问题 | 状态 | 落实位置 |
|---|---|---|---|
| 24 | "代码改了但界面没变"反复发作 | ✅ | 首页 HTML `Cache-Control: no-store` + 资源内容哈希 + 右下角构建标记；🆕 标记改为**实际下发 index.html 的内容哈希**（每次构建必变），`/api/health.frontend_build` 可远程比对 |
| 25 | 需求缺登记、易漏 | 🆕 | 本文档 |

## 待办

- 无（本轮五项全部完成）。若用户回顾出新的遗漏项，追加到本表并标注 ⏳。
