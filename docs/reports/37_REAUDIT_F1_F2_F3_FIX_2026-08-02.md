---
title: 系统红队复审残余 P1 修复报告（第六轮复审 F1/F2/F3，2026-08-02）
status: approved
category: reports
created: 2026-08-02
last-reviewed: 2026-08-02
supersedes: reports/36_SYSTEM_RED_TEAM_REAUDIT_2026-08-02.md
---

# 系统红队复审残余 P1 修复报告（2026-08-02）

> 关闭 `docs/reports/36`（第六轮独立复审，BLOCK）的 3 个残余 P1：
> F1/F2（P1-A 季度单季值，常态场景系统性错误）与 F3（P1-C CLI→保存→
> 导出链路截断标记丢失）。全部修复附带回归测试并经真实调用序验证。
> 门禁：S1 **406 passed**（+4）、Ruff 零错误、前端 lint/52 node/10 vitest/build、
> `uv lock --locked`。

## 关闭清单

| 编号 | 问题（复审实测） | 修复 | 回归测试 |
|---|---|---|---|
| F2 | `prior_by_year` 在行被原地差分**之后**取值 → 下一季度减去的"上期累计"实为上期**单季值**；同一年 ≥3 个连续报告期时 Q3/Q4 系统性错误（实测 100/230/400/580 → 100/130/270/310，真值 100/130/170/180） | `app/web/api/stock_detail.py` `_to_single_quarter`：先捕获差分**前**的累计原值 `cumulative`，单季值 = 本期累计 − 上期累计；`prior_by_year` 存入累计原值（供下期差分） | `test_stock_detail_periods.py::test_quarterly_trend_uses_cumulative_prior_not_single_quarter_value`（4 连续季度断言 100/130/170/180） |
| F1 | 年内首行不是 Q1 时累计值被直通为单季值（实测年起 Q2，累计 230/400/580 → Q2 输出 230） | 年内首行 `quarter != 1` → 单季值不可推导，fail-closed 置 NULL；首行即 Q1 保留（累计=单季） | `test_quarterly_trend_year_starting_at_non_q1_fails_closed`（Q2 NULL、Q3 170、Q4 180） |
| F3 | CLI `screening run` 的 `confidence_summary` 不含 `truncated`/`total` → `save_result` 原样复制 → web 导出读不到 → 5,000 行无 `_truncated` 列的静默截断导出 | `app/cli/main.py` `screening_run` 的 `confidence_summary` 增加 `truncated` 与 `total`（与 web 路径对齐）；导出读取逻辑不变即生效 | `test_research_path_integrity.py::test_cli_run_to_web_export_preserves_truncated_marker`（完整链路：CLI run（truncated=True/total=10）→ save_result → 持久化 summary 断言 → web export CSV 含 `_truncated` 列且行尾 True） |
| P2-1（级联过空） | 仅缺 Q2（Q1/Q3/Q4）时 Q4 也被级联置 NULL | F2 修复后 Q4 = Q4累计 − Q3累计 可推导，级联消除 | `test_quarterly_trend_mid_year_gap_keeps_later_quarters_derivable`（Q3 NULL、Q4 240） |

## 验证门禁（2026-08-02）

| 门禁 | 结果 |
|---|---|
| `scripts/s1-pytest.ps1 tests/regression -q --no-header` | **406 passed**（新增 4 项） |
| `uv run --locked ruff check app tests/regression` | All checks passed |
| 前端 lint / node 合约 / vitest 组件 / build | 通过 / 52 passed / 10 passed / 成功 |
| `uv lock --locked` | 通过 |

## 状态

- 第六轮复审 F1/F2/F3 与级联过空项全部关闭；`reports/34`/`reports/35`/`reports/36`
  修复事实保留，裁决更新为本报告。
- 未关闭的披露项：`reports/36` §5 其余 P2（适配器静默截断批次以 strict 记录
  lineage 的溯源矛盾 minor、存量结果无法追溯回填 truncated——5000 行结果
  无法区分"恰好 5000"与"被截断"，如实披露不伪造；计数查询约 ×2 成本仍在
  PRD §19.1 5 秒 SLA 内）；`reports/34` §5 次要 P2 与数据披露缺口不变。
