---
title: 距正式使用问题清单
status: superseded
category: reports
last-reviewed: 2026-07-28
superseded-by: reports/25_FORMAL_ACCEPTANCE_STATUS_2026-07-30.md
---

# 距正式使用问题清单

> 审查日期：2026-07-28（更新）
> 数据状态：CSMAR (1990-2025Q1) + AKShare (2025Q2+) 已导入
> 审查结论：**BLOCK** — 6 个 P0 阻断 + 14 个 P1 功能缺陷，不能上线
> 
> **2026-07-28 更新说明：** 五条独立审计链并行完成（存储安全、指标计算、CLI/API/备份、前端验收、构建交付），发现多个此前报告完全未覆盖的 P0 级问题。`docs/21` 的"愿以全部身家担保"结论被证伪。

---

## 一、数据层

### P0 - 阻断使用

| # | 问题 | 现状 | 影响 |
|---|------|------|------|
| D1 | **stock_meta 元数据全部 NULL** | listing_date / sw_level1 / sw_level2 / is_st / is_suspended / pinyin 全部 100% NULL | 行业排名、ST 过滤、拼音搜索全部不工作 |
| D2 | **扣非净利润覆盖率仅 6%** | income_statement.deducted_net_profit: CSMAR 时期 356K 行全 NULL，仅 AKShare 补充的 20K 行有值 | 扣非 PE/ROE 等指标不可用 |

### P1 - 影响功能

| # | 问题 | 现状 | 影响 |
|---|------|------|------|
| D3 | **basic_eps 缺失 13%** | 362K/377K 非空，约 15K 行（主要是 2007 年之前）无 EPS | 老股票的 PE 计算不完整 |
| D4 | **分红 48% 为占位日期** | 50,359 行 ex_date 为 12-31 或 06-30（来自 CSMAR） | 股息率、连续分红年数不可靠 |
| D5 | **raw/QFQ 价格覆盖不一致** | raw: 5,541 只, qfq: 5,200 只, 差 341 只 | 前复权 K 线部分股票不可用 |
| D6 | **xdxr 表为空** | 0 行除权除息事件数据 | 复权计算精度受影响 |

### P2 - 数据治理

| # | 问题 | 现状 |
|---|------|------|
| D7 | SQLite 遗留数据 | job_logs(4)、screening_results(5)、manual_overrides(8)、fetch_batch(133) 来自旧系统 |
| D8 | staging 残留表 | indicator_snapshot_staging_* 表未清理 |
| D9 | source_audit 陈旧 | 15,649 行来自旧版导入，无法形成真实溯源 |

---

## 二、指标计算

### P0

| # | 问题 | 现状 | 影响 |
|---|------|------|------|
| I1 | **指标快照不完整** | 用简化 SQL 算的，缺 YoY、CAGR、MA5~250、连续分红年数等 | 筛选和个股页缺少重要指标 |

---

## 三、代码/工程

### P0

| # | 问题 | 现状 | 修复方向 |
|---|------|------|---------|
| C1 | **DI 迁移阻断 CLI** | `python -m app.cli.main` 需要手工设 VD_ENV 等多个环境变量，否则 `PathIsolationError` | Config.load() 应能从配置文件自动创建 DatabasePathSet |
| C2 | **config/user.yaml 不存在** | 配置缺失，用户无法覆盖默认值 | 创建模板 |

### P1

| # | 问题 |
|---|------|
| C3 | 回归测试 31 个 ERROR（数据库占用导致，非代码 bug，但需要重跑确认） |
| C4 | 无自动化数据更新流程 |

### P2

| # | 问题 |
|---|------|
| C5 | app/web/static/ 堆积大量旧版构建产物，与 frontend/dist/ 不同步 |
| C6 | 无 E2E 测试 |

---

## 四、前端

### P2

| # | 问题 | 现状 |
|---|------|------|
| F1 | UI 简陋（用户反馈） | 待设计改进 |
| F2 | 缺少 eslint（未安装） | lint 脚本无法运行 |

---

## 七、存储与路径安全（2026-07-28 新增）

### P0 - 阻断使用

| # | 问题 | 文件:行号 | 影响 |
|---|------|-----------|------|
| **S1** | `from_env()` 在环境变量缺失时**默认写入正式库**，而非抛异常 | `path_policy.py:81-89` | 测试/CLI 误操作直接污染生产数据。代码自动设置 `VD_ENV=formal` 和 `VD_FORMAL_ACK=confirmed`，绕过安全检查 |
| **S2** | 备份恢复**不是跨表原子的** — 每个表独立事务，部分失败导致半新半旧 | `backup/manager.py:491-499` | 恢复失败后数据库处于不一致状态：部分表是新数据，部分表是旧数据 |
| **S3** | 离线恢复密钥**从未参与加密** — 数据用密码加密，恢复密钥无法解密 | `backup/manager.py:336-374` | PRD §18.3 的离线恢复功能**完全失效**。用户忘记密码时无法使用恢复密钥恢复数据 |

### P1 - 影响功能

| # | 问题 | 文件:行号 | 影响 |
|---|------|-----------|------|
| S4 | 非 Windows 环境凭据明文存储 | `backup/manager.py:148-155` | 违反 PRD §18.3 AR12 |
| S5 | 备份创建时未获取写锁 | `backup/manager.py:316-328` | 备份可能包含不一致数据 |
| S6 | 恢复时未验证个性化数据表名白名单 | `backup/manager.py:521-534` | 恶意备份可覆盖系统表 |

### P2 - 代码质量

| # | 问题 | 文件:行号 |
|---|------|-----------|
| S7 | DuckDB `execute_script()` 未使用事务 | `duckdb_store.py:182-185` |
| S8 | SQLite `executescript()` 破坏事务原子性 | `schema.py:539-563` |

---

## 八、指标计算精度（2026-07-28 新增）

### P1 - 影响功能

| # | 问题 | 文件:行号 | 影响 |
|---|------|-----------|------|
| **I2** | TTM 用 `rows[4]` 按位置匹配而非日期，报告期缺失时计算错误 | `calculator.py:339` | PE/PS/PCF 偏差。如果公司重述历史数据导致某一期缺失，`rows[4]` 可能不是去年同期 |
| **I3** | 股息率用 2 年窗口 SUM(DPS)，可能偏高 50-100% | `calculator.py:483-494` | 股息率指标不准确。注释承认此问题但未修复 |
| **I4** | `_write_batch` 用第一条记录的字段决定写入列，后续记录的额外字段被丢弃 | `calculator.py:970` | 部分指标静默丢失 |
| **I5** | 增量更新只写 raw 价格，不更新 qfq | `update.py:260-291` | qfq 数据在 init 后逐渐陈旧 |

### P2 - 代码质量

| # | 问题 | 文件:行号 |
|---|------|-----------|
| I6 | `paid_in_capital` 被查询但从未使用，是死代码 | `calculator.py:207` |
| I7 | `total_dps` 变量名误导（实际是历史总和，非最近 12 个月） | `calculator.py:467-470` |
| I8 | `_calc_consecutive_div_years` 使用 `datetime.now().year`，年初可能少计 1 年 | `calculator.py:881` |

---

## 九、前端验收流程（2026-07-28 新增）

### P0 - 阻断验收

| # | 问题 | 文件:行号 | PRD 条款 | 影响 |
|---|------|-----------|----------|------|
| **F1** | 筛选页**草稿自动恢复完全缺失** — 无 localStorage/后端草稿表 | `ScreeningPage.vue:45-53` | §12.1 | 验收流程一步骤 2 "最近草稿可自动恢复" **必定失败** |
| **F2** | 排名数据后端计算但**前端完全不请求、不展示** | `ScreeningPage.vue:114-118` + `engine.py:229-249` | §12.2 步骤4 | 验收流程一步骤 4 "条件中可混用排名" **验收失败** |

### P1 - 影响验收

| # | 问题 | 文件:行号 | PRD 条款 | 影响 |
|---|------|-----------|----------|------|
| F3 | "仅查看 strict" 开关基于 null 过滤而非置信度 | `ScreeningResultsPanel.vue:72-85` | §9.1 | 功能与 PRD 要求不符 |
| F4 | CSV 导出缺少规则版本/指标版本/置信度 | `screening.py:157-169` | §12.5 | 验收流程一步骤 8 失败 |
| F5 | 保存结果时 rule_id/rule_version 硬编码为 None/1 | `screening.py:84-99` | §12.5 | 无法追溯到规则版本 |
| F6 | DSL 生命周期只有 3 态，缺单股预览和小样本预览 | `DslIndicatorManager.vue:16` | §11.5 | 验收流程一步骤 3 失败 |
| F7 | IndicatorTabs 时间维度切换器**无实际效果** — 事件未被父组件监听 | `IndicatorTabs.vue:27-41` + `StockDetailPage.vue:299` | §14/§20.2 步骤6 | 验收流程二步骤 6 部分失败 |
| F8 | 溯源信息缺少生效日期/数据版本/公式/as_reported 差异 | `DataTraceability.vue:27-39` | §14 | 验收流程二步骤 8 不完整 |

### P2 - 代码质量

| # | 问题 | 文件:行号 |
|---|------|-----------|
| F9 | PDF 冷归档恢复指引显示为原始 JSON | `DataTraceability.vue:66-68` |

---

## 十、构建与交付（2026-07-28 新增）

### P0 - 阻断交付

| # | 问题 | 文件:行号 | 影响 |
|---|------|-----------|------|
| **B1** | 前端构建产物与 PyInstaller 打包目录**无自动同步** | `vite.config.ts` + `value-dashboard.spec:15-17` | 发布后可能打包旧版前端。当前恰好同步但无自动化保障 |

### P1 - 影响交付

| # | 问题 | 文件:行号 | 影响 |
|---|------|-----------|------|
| B2 | `database_paths` fixture 在无 wrapper 时必然崩溃 | `tests/conftest.py:24` | 标准 pytest 工作流不可用，必须使用专用 PowerShell 脚本 |
| B3 | `value-dashboard.spec` 的 `hiddenimports` 可能遗漏 `pyarrow` 和 `typer` | `value-dashboard.spec:31-48` | 打包后的 exe 可能在特定操作路径上崩溃 |
| B4 | `app/web/static/` 堆积大量旧构建产物 | `app/web/static/assets/` | PyInstaller 打包产物体积膨胀 |

### P2 - 代码质量

| # | 问题 | 文件:行号 |
|---|------|-----------|
| B5 | `pyproject.toml` 的 `testpaths` 配置正确，但缺少 `--basetemp` 防护 | `pyproject.toml:53-54` |
| B6 | `start.bat` 的开发模式回退路径缺少 `__main__.py` 保护 | `start.bat:38` |

---

## 十一、PRD 验收流程判定（2026-07-28 新增）

| 验收流程 | 判定 | 阻断原因 |
|----------|------|----------|
| **流程一：当前筛选** | **不通过** | F1 草稿恢复缺失、F2 排名不可见、F3/4/5/6 多个功能不完整 |
| **流程二：单股研究** | **有条件通过** | K 线/指标/趋势核心功能完整；F7 时间切换无效、F8 溯源不完整 |
| **流程三：CLI 与 OpenCode** | **有条件通过** | CLI 可用；F6 DSL 生命周期不完整 |
| **流程四：初始化/更新/备份/恢复** | **不通过** | S2/3 备份恢复有致命缺陷、S4/5/6 安全与完整性问题 |

---

## 十二、现有报告纠正（2026-07-28 新增）

| 现有报告声称 | 实际验证 |
|-------------|----------|
| `docs/17` P0-2：`paid_in_capital` 被错误当作总股本 | **误报** — 代码正确从 `stock_meta.total_shares` 获取总股本 |
| `docs/17` P0-7：`confirmed_at` 列不存在 | **已修复** — 当前代码无此问题 |
| `docs/19`：数据收集后可立即使用 | **错误** — 备份恢复有 P0 缺陷，前端验收不通过 |
| `docs/21`：环境变量问题已修复，应用可启动 | **部分正确** — 启动可工作，但 `from_env()` 默认写正式库是新的安全隐患（S1） |
| `docs/21`：愿以全部身家担保 | **错误** — 只有源码推演，无运行证据；遗漏多个 P0 |

---

## 十三、修复优先级建议（更新）

```
第一轮（P0 阻断，必须修复才能上线）：
  S1 path_policy.py fail-closed
  S2/S3 备份恢复原子性 + 离线恢复密钥
  F1/F2 前端草稿恢复 + 排名数据打通
  B1 前端构建自动同步

第二轮（P1 重要，影响验收完整性）：
  I2/I3/I4 指标计算精度
  F3/F4/F5/F6 strict 过滤 + CSV 溯源 + 规则版本 + DSL 生命周期
  F7/F8 时间切换 + 溯源字段
  S4/S5/S6 凭据存储 + 备份锁 + 表名验证
  B2/B3/B4 测试 wrapper + hiddenimports + 旧产物清理

第三轮（P2 治理）：
  S7/S8 事务改进
  I6/I7/I8 代码质量
  F9 PDF 恢复指引
  B5/B6 测试防护
  D7/D8/D9 数据清理
  C5/C6 静态同步 + E2E 测试
```

---

## 十四、验证标准（更新）

修复完成后，以下操作应全部通过：

1. `pytest tests/regression/` — 无需 wrapper 脚本，100% pass
2. `vd data compute_indicators` — 无需手动设环境变量
3. `vd server` — Web 服务器正常启动
4. 筛选页 → PE<20, ROE>15% → 返回结果，**草稿可自动恢复**
5. 筛选页 → 条件中可混用**全市场排名和行业排名**
6. 个股页 → 茅台(600519) → K线+指标正常显示，**时间维度切换有效**
7. 个股页 → 溯源信息包含**生效日期/数据版本/公式**
8. 数据状态页 → 无阻断级警告
9. CLI `backup create` → `backup restore` → **数据完整一致**
10. CLI `backup create` → 忘记密码 → **使用恢复密钥成功恢复**
11. `npm run build` → 产物**自动同步**到 `app/web/static/`
12. PyInstaller 打包 → `start.bat` → **前端版本正确**
